[tox]
requires = ["tox=>4.50"]
env_list = [
  "3.13",
  "3.12",
  "3.11",
  "3.10",
  "3.9"
]

skip_missing_interpreters = false

[env_run_base]
description = "run tests with python unittest module"
commands = [
    [
        "python", "-m", "unittest"
    ]
]